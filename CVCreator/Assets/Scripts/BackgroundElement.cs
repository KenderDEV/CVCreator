using UnityEngine.UI;
using UnityEngine;

public class BackgroundElement : MonoBehaviour
{
    public enum BackgroundType { Left, Right }
    public BackgroundType backgroundType;

    public Image backgroundImage;
}
